# Fliqlo Timer Dependencies
# No external dependencies required - uses only Python standard library
# tkinter (included with Python)
# pygame (optional, for sound effects - install if needed)
pygame>=2.0.0